{
    "name": "gotosocial-comment-embed",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev:worker": "bun run --cwd packages/worker dev",
        "dev:embed": "bun run --cwd packages/embed dev",
        "build": "bun run --cwd packages/embed build",
        "deploy": "bun run --cwd packages/worker deploy"
    }
}