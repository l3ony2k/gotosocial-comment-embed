{
    "name": "@gts-embed/embed",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "bun run build --watch",
        "build": "bun build src/l3on-comments.ts --outdir dist --minify"
    }
}
